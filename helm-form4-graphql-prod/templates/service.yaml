apiVersion: v1
kind: Service
metadata:
  name: form4-graphql-svc
  labels:
    app: form4-graphql
spec:
  type: ClusterIP

  selector:
    app: form4-graphql

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
